{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "LLDB: Run C/C++",
            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "C/C++: clang++ build active file"
        }
    ]
}
